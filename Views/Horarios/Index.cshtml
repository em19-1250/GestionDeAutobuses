@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}



@model IEnumerable<GestionAutobusesAPI.Models.Horario>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AutobusId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RutaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaHoraSalida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaHoraLlegada)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AutobusId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RutaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaHoraSalida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaHoraLlegada)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
